Readme

1. The file named binary_online.py is the code for 5.1 and file named multi_online.py is the code for the 5.2
2. The version for python is 2.7.12
3. The libraries on which the code depends on are:
   numpy, can be installed using either pip or apt-get install python-numpy
   matplotlib. Rest are libraries already in python.
4. For Passive aggressive, in each file, the training function is called by giving the argument 1 and 0 for Standard Perceptron.
5. For averaged perceptron there is another train finction that is called named as "classifier_train_average_perceptron" and it is called without argument.
6. The data is to train the classifier is given as an argument, to the variables, path1, and path2.



